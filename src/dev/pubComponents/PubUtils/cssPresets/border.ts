import {CSSProperties} from "react";

// ============================================ 边框样式预设 ============================================

/**
 * 边框样式 - 实线
 */
export const borderSolid: CSSProperties = {
    borderStyle: "solid"
}

/**
 * 边框样式 - 虚线
 */
export const borderDashed: CSSProperties = {
    borderStyle: "dashed"
}

/**
 * 边框样式 - 点线
 */
export const borderDotted: CSSProperties = {
    borderStyle: "dotted"
}

/**
 * 边框样式 - 无
 */
export const borderNone: CSSProperties = {
    borderStyle: "none"
}
